# ACTIVE-UX — UX lane (single slot)

> **Lane B — UX.** Only **one agent at a time** may hold this slot. App-code work serializes by design — the "seven-sweep" incident (2026-05-17) proved parallel agents on `src/js/` collide repeatedly.
>
> Read `00_meta/LANES.md` for the lane definition and `00_meta/VIEW-CONTRACT.md` for the rules any view module must satisfy. Do **not** stage any path under `01_*` through `17_*` content folders. The pre-commit hook will refuse a cross-lane commit.

## Slot

| Handle | Scope | Owned files (explicit) | Started | Status |
|---|---|---|---|---|
| opus-alphabets-section-2026-06-10 | ALPHABETS V2 Section (John ratified N1: master-pill 5→6, "a Section like ATLAS and TIMELINE for alphabets with all alphabets"). Per AUDIT/2026-06-10-alphabets-page-plan.md: Phase 0 carve (VIEWS.alphabets + .alphabets-pane skeleton + pill entry + teardown) then Phase 1 genealogy tree (ALL 42 type:alphabet nodes from VAULT_DATA, time-on-X SVG DAG, normalized descent edges, branch rows, selectNode click-through). No legacy chrome/aesthetic; SVG (few-large); screenshot-verified per rule #8. | `src/js/views/alphabets.js` (new), `src/js/app.js` (VIEWS.alphabets + teardown sweep), `src/js/app-pill.js` (MASTER_VIEWS 6th entry), `index.html` (script tag + cache-bust), `00_meta/STATUS.md`, `00_meta/ACTIVE-UX.md` | 2026-06-10 | FINISHED 2026-06-10 (commit 2fbe7d11 — screenshot-verified; regression: forge/boards/alphabets all mount, teardown symmetric, 0 console errors) |
| watcher-claude-phase-23-1b-legend | Phase 23.1b carve: `wireLegend` (282 LOC) → `src/js/forge/legend.js`. forge.js 8237→7964 (-273 LOC). Same lift-and-shift pattern as 23.1a, boundary contract `window._forgeLegend.attach({ local, recomputeFocus })`. Preview-verified: panel opens fully with 7 wire-bucket rows + 5 tier toggles + political-risk row, LS-derived state correct, zero console errors. | `src/js/forge/legend.js` (new), `src/js/views/forge.js` (stub), `index.html` (script tag + cache-bust on changed files only) | 2026-05-25 LATE | FINISHED 2026-05-25 LATE evening |
| watcher-claude-phase-23-1a-timeline-scrubber | Phase 23.1a — first forge.js carve. Lift `wireTimelineScrubber()` (353 LOC, lines 7589-7941) out of forge.js into a dedicated module `src/js/forge/timeline-scrubber.js`. Pure refactor, no behavior change. forge.js 8577 → 8237 (-340 LOC). Boundary contract: `window._forgeTimelineScrubber.attach({ local, recomputeFocus, saveRuntimeState })`. Preview-verified: pixel-identical render, scrubber state computed correctly (IN box "+9000 BCE", OUT box "2026 CE", PRESENT box "3487 BCE"), zero console errors. | `src/js/forge/timeline-scrubber.js` (new), `src/js/views/forge.js` (function body replaced with stub), `index.html` (script tag added before forge.js, cache-bust bumped for changed files only) | 2026-05-25 LATE | FINISHED 2026-05-25 LATE evening |
| watcher-claude-dev-overview-classifier-rigor-2026-05-29 | Rigor pass on the family classifier in `scripts/build_health_index.py`. Audit the 196 nodes currently failing family classification, decide per-value whether to (a) extend matchers, (b) leave genuinely cross-tradition, (c) data-quality flag. Discipline: minimal-but-complete matcher additions, no band-aid bolt-ons. Add false-positive cross-check + INTEGRITY block in JSON. Regenerate + commit. | scripts/build_health_index.py, src/data/health-index.json | 2026-05-29 | started |
| watcher-claude-dev-overview-2026-05-29 | Build the DEV OVERVIEW panel John asked for: macro view of all intel coverage — every lens + every family — with VALUES + "health" status vs a scholarly ballpark baseline. Baseline reference: Encyclopedia of Religion (Lindsay Jones ed., Macmillan, 2nd ed. 2005) + named supplements per lens where EoR under-covers (Greer 2003 for esoteric, OCD for Greco-Roman, etc.). Methodology documented inline in the panel. Health bands: <25% red (anemic) / 25-60% amber (developing) / >60% green (rich). Implementation: `scripts/build_health_index.py` walks all 28 content folders, counts nodes per lens, maps `tradition:` values to 14 canonical FAMILIES via curated rules, emits `src/data/health-index.json`. New view module `src/js/views/dev-overview.js` renders a full-page overview. Wired under the `✦ user-menu` (account drawer) → new "Dev" section → "Overview" item. Panel is operator-facing — fine for the current pre-SaaS phase per the existing dev-panel.js precedent. | `scripts/build_health_index.py` (new), `src/data/health-index.json` (new, generated), `src/js/views/dev-overview.js` (new), `src/js/user-menu.js` (new menu section), `src/styles/app.css` (new view styles), `index.html` (script tag + cache-bust) | 2026-05-29 | started |
| watcher-claude-phase-24-legacy-v1 | Phase 24 — Legacy/Archive viewer V1 per `AUDIT/2026-05-25-phase-24-legacy-viewer-spec.md`. New side-nav pill → list of closed specs + closed audits + archived STATUS + archived HANDOFFs (4 categories, read-only browse, click → markdown render in side panel). NOT historical site snapshots (V1.1). Reuses canonical primitives per SEVERITY DOGMA §5.7. **John greenlit "yes" 2026-05-25 LATE** after preview verification. 68 entries indexed, all category groups render, markdown converter handles tables/headers/code/bold/italic/wikilinks-as-spans, active-item highlight works, zero console errors. Boot-race patched. | `src/js/views/legacy.js` (new), `src/js/app.js`, `index.html`, `src/styles/app.css`, `scripts/build_legacy_index.py` (new), `src/data/legacy-index.json` (new) | 2026-05-25 LATE | FINISHED 2026-05-25 LATE evening |

_Slot is taken._

### Recently released
- `opus-forge-rebuild-phase-5c-2026-05-20` (2026-05-20) — **Phase 5C — node + glyph opacity UNIFIED on GPU.** Triggered by John's architectural critique: "NODE that just transforms properties in SIMPLE TERMS AVOIDING BUGS" + slow pan. Both traced to the disk/glyph dual opacity pipeline (uniform-driven in GPU for disks, JS per-frame loop for glyphs — drift possible, JS loop slowed pan). Fix: GLYPH_SHADER fragment now computes `a = tex.a × tint.a × glyph_op × dim_mult` where `dim_mult` mirrors the disk shader exactly. New `glyph_params` vec4 added to View uniform (repurposed unused 8th bucket slot; EDGE_SHADER clamp now 0..6). **DELETED** the refreshGlyphAlphas function (~80 lines), the per-frame O(N) JS loop, the glyphInstancesDirty flag + all 4 setter sites (camera.onChange / recomputeFocus / tickNodeFades / rebuildGlyphInstanceBuffer), and the redundant off-viewport CPU cull (WebGPU vertex-clip handles it free). Glyph alpha column now = 1.0 at rebuild, never touched per frame. One opacity logic, one place — drift impossible by construction. ~110 lines of code removed net. Verified live: deities boots clean, FRAME 0.1 ms, atlas {640, 512, mip 10}, Zeus locked from LS still hydrates with glow halo + focused 1-hop neighbors visible with glyphs, pan/zoom smooth, zero console errors. Cache-bust `20260520-rebuild-phase-5c`. Files: `src/js/views/forge.js`, `src/js/engine/renderer/webgpu.js`, `index.html`.
- `opus-forge-fx2-cullfix-2026-05-20` (2026-05-20) — **post-Phase-4B fix.** John caught glyphs missing on disks ("empty circles"). Root cause: FX2 cull threshold 4 px > node-min-screen-px-clamp floor (3 × glyph_scale 0.85 = 2.55 px). Every clamp-floor glyph was culled while its disk remained visible. Fix: dropped the min-size cull entirely; off-viewport cull stays. Files: `src/js/engine/renderer/webgpu.js`, `index.html`. Cache-bust `20260520-rebuild-phase-5b-cullfix`.
- `opus-forge-rebuild-phase-5b-2026-05-20` (2026-05-20) — **Phase 5B — MANAGEMENT layer locked. FOUNDATION REBUILD COMPLETE.** All 5 REQUIRED + 2 RECOMMENDED items from [`AUDIT/forge-rebuild-5A-management-2026-05-20.md`](../AUDIT/forge-rebuild-5A-management-2026-05-20.md). Safe defaults: Q1=A all-three-persisted, Q3=A disclosure-group, Q4=tune-now, Q5=eager. **(M-F1)** packedAtScale written immediately after rebuildForMode's fitToExtent — kills the spurious rebake-on-old-mode class. **(M-F2)** Single LS key `codex-atlas/forge-runtime-v1` carries mode+timeline+lockedSet. Hydrated at mount; saved on mode-change/scrubber-pointerup/toggleLock. Verified: lock Zeus → reload → restored. **(M-F3)** wireTimelineScrubber split — boot wires DOM, `local.scrubber.refreshBounds()` re-derives bounds + preserve-or-clamp on each rebuildForMode. **(M-F4)** N-aware drift threshold (5/15/30% per scale tier). **(M-F5)** Eager `buildSearchIndex()` at end of rebuildForMode; findBestMatch walks precomputed index. **(M-F6)** Side-nav: Forge promoted to primary with active-focus pill; other 25 tabs collapsed behind `<details>More views</details>` disclosure. **(M-F7)** resizeAndFit zero-size bail. **(spec-lock header)** ~110-line MANAGEMENT section in forge.js. **New _forgeDebug**: timeline, countRebakeNodes, dumpRuntime, dumpLsRuntime. Verified live: boots clean FRAME 0.0 ms, side-nav demoted correctly, LS round-trip works, all prior invariants still pass. Cache-bust `20260520-rebuild-phase-5b`. Files: `src/js/views/forge.js`, `index.html`, `src/styles/app.css`. **Foundation done.** Next: Phase 6 autonomous polish (only when greenlit).
- `opus-forge-fx5-floor-fix-2026-05-20` (2026-05-20) — **post-Phase-4B fix.** John caught the glow square-clip artifact returning on Mastema/Mullissu/Fujin/Raijin. Root cause: the FX5 derived formula `0.05 + max(0, strength-1.0)*0.1` gave 0.05 at default strength=0.5, LOWER than the proven-safe 0.15. Fix: `0.15 + max(0.0, strength - 0.5) * 0.20` — restores 0.15 floor at default, scales UP past it (not below). Lesson saved as `feedback_audit_formulas_need_default_check.md`. Verified live: Mullissu at 593% zoom — glow halo circular, no square mask. Files: `src/js/engine/renderer/webgpu.js`, `index.html`. Cache-bust `20260520-rebuild-phase-4b-fx5fix`. per [`AUDIT/forge-rebuild-5A-management-2026-05-20.md`](../AUDIT/forge-rebuild-5A-management-2026-05-20.md). REQUIRED: M-F1 packedAtScale write at fit site; M-F2 LS persistence under `codex-atlas/forge-runtime-v1` (Q1=A: mode+timeline+lockedSet); M-F3 split wireTimelineScrubber + refreshScrubberBounds; M-F6 side-nav disclosure group "More views" + Forge active-focus pill (Q3=A safe default); M-F7 resizeAndFit zero-size bail; MANAGEMENT spec-lock header section. RECOMMENDED: M-F4 N-aware drift threshold (Q4=tune now); M-F5 eager search index (Q5=eager). New _forgeDebug: timeline, countRebakeNodes. First Phase since 0 that touches app.js + index.html + app.css (side-nav) — broader blast radius than 1B-4B. Acceptance gates: AT-1..AT-13 per audit §6. | `src/js/views/forge.js`, `src/js/engine/renderer/webgpu.js` (counter), `src/js/app.js` (side-nav scope only), `index.html` (side-nav + cache-bust), `src/styles/app.css` (side-nav demote), `00_meta/STATUS.md`, `00_meta/ACTIVE-UX.md` | 2026-05-20 | started |

_Slot is open. Claim it by filling the row above. If the slot is taken, switch to investigation work or wait._

### Recently released
- `opus-forge-fx5-floor-fix-2026-05-20` (2026-05-20) — **post-Phase-4B fix.** John caught the glow square-clip artifact returning on Mastema/Mullissu/Fujin/Raijin. Root cause: the FX5 derived formula `0.05 + max(0, strength-1.0)*0.1` gave 0.05 at default strength=0.5, LOWER than the proven-safe 0.15. Fix: `0.15 + max(0.0, strength - 0.5) * 0.20` — restores 0.15 floor at default, scales UP past it (not below). Lesson saved as `feedback_audit_formulas_need_default_check.md`. Verified live: Mullissu at 593% zoom — glow halo circular, no square mask. Files: `src/js/engine/renderer/webgpu.js`, `index.html`. Cache-bust `20260520-rebuild-phase-4b-fx5fix`.
- `opus-forge-rebuild-phase-4b-2026-05-20` (2026-05-20) — **Phase 4B of the layered Forge rebuild — FX layer locked.** All 5 REQUIRED + 3 RECOMMENDED + 1 OPTIONAL items from [`AUDIT/forge-rebuild-4A-fx-2026-05-20.md`](../AUDIT/forge-rebuild-4A-fx-2026-05-20.md). Safe-default Q4=delete on glyph_tint. **(FX1)** glyphInstancesDirty flag — drawFrame gates refreshGlyphAlphas + GPU writeBuffer; ~21 KB/frame saved at idle. **(FX2)** glyph viewport + min-size cull (screen_r < 4px OR off-viewport → alpha=0); 676 deities glyphs cull at fit-zoom. **(FX3)** label DOM pre-create cap = min(N, label_idle_max + label_cap × 2) ≈ 990; lazy-rest via ensureLabelEl. **(FX4)** atlas 128 px + 10 mip levels; sharper Retina deep-zoom; cost ~280 KB. **(FX5)** selected-glow discard derived from selected_glow_strength uniform (square-clip class permanently closed). **(FX6)** visibleLabelEls Set drives label diff + position loops (~10k attribute reads → ~100). **(FX7)** glyph_tint DELETED from PARAM_DEFAULTS (Q4=delete, frozen at 0.55 literal). **(FX8)** GLYPH_SHADER header comment rewritten to match current z=disk-z + cross-pipeline invariant. **(FX9)** syncGlyphPositions/syncGlyphFocus no-op stubs DELETED + callers cleaned. **(spec-lock)** 90-line FX section in header. **New _forgeDebug**: countGlyphVboWrites, countCulledGlyphs, dumpAtlasInfo. **Bug caught mid-phase**: backticks in a comment inside the NODE_SHADER WGSL template closed the template literal early; rewrote comment to avoid them. Verified live: deities boots clean, FRAME 0.1 ms, atlas `{640×512, mip 10}`, glyph+edge+node VBO writes all stable at idle (deltas=0 over 500ms), zero console errors. Cache-bust `20260520-rebuild-phase-4b`. Files: `src/js/views/forge.js`, `src/js/engine/renderer/webgpu.js`, `src/js/engine/graph/glyph.js`, `index.html`. Next: Phase 5A — MANAGEMENT micro-audit. Lands the 5 REQUIRED + 3 RECOMMENDED + 1 OPTIONAL items from [`AUDIT/forge-rebuild-4A-fx-2026-05-20.md`](../AUDIT/forge-rebuild-4A-fx-2026-05-20.md): FX1 refreshGlyphAlphas settled-fade short-circuit + null-glyphInstances gate; FX2 glyph viewport+min-size cull; FX3 label DOM pre-create N-aware cap; FX4 atlas 128px + mipmaps (downsampled canvases); FX5 selected-glow discard derived from uniform; FX6 visible-labels Set; FX8 rewrite stale GLYPH_SHADER comment; FX9 delete syncGlyphPositions/syncGlyphFocus no-ops; FX7 DELETE glyph_tint from PARAM_DEFAULTS (Q4=delete per Phase 0 consistency). FX section in spec-lock header. New _forgeDebug: countGlyphVboWrites + dumpAtlasInfo + countCulledGlyphs. First Phase since 0 with visibly different output (sharper glyphs at deep zoom + smoother large-mode mounts). | `src/js/views/forge.js`, `src/js/engine/renderer/webgpu.js`, `src/js/engine/graph/glyph.js`, `index.html` (cache-bust), `00_meta/STATUS.md`, `00_meta/ACTIVE-UX.md` | 2026-05-20 | started |

_Slot is open. Claim it by filling the row above. If the slot is taken, switch to investigation work or wait._

### Recently released
- `opus-forge-rebuild-phase-3b-2026-05-20` (2026-05-20) — **Phase 3B of the layered Forge rebuild — WIRES layer locked.** R1-R6 required + D1-D4 recommended from [`AUDIT/forge-rebuild-3A-wires-2026-05-20.md`](../AUDIT/forge-rebuild-3A-wires-2026-05-20.md). Safe-default Q1=B (keep bucketPalette cache + invariant comment since PARAM_DEFAULTS is frozen). **(R1)** edgeInstancesDirty flag mirrors N2; gates writeBuffer on `dirty || grew`; verified delta=0 over 500ms idle (~130 MB/s saved at 10k). **(R2)** DELETED forceWriteEdgeState (dead-code post convention-flip). **(R3)** DELETED rebakeBucketPalette (orphan post Phase 0). **(D1)** BUCKET_ORDER derived from BUCKET_INDEX SSOT. **(D2)** gradient-on-color-not-alpha lock comment. **(D3)** endpoint-offset lock comment (nodeRadii REQUIRED). **(D4)** edge_t direction docstring. **(R4)** 86-line WIRES section in spec-lock header. **(R5)** edgesAndNodesOnly toggle. **(R6)** countEdgeVboWrites. Verified live: deities boots clean, FRAME 0.8 ms, zero console errors, edge+node dirty-flag gates both stable. Cache-bust `20260520-rebuild-phase-3b`. Files: `src/js/views/forge.js`, `src/js/engine/renderer/webgpu.js`, `src/js/engine/graph/edge.js`, `index.html`. Next: Phase 4A — FX micro-audit.

### Recently released
- `opus-forge-rebuild-phase-2b-2026-05-20` (2026-05-20) — **Phase 2B of the layered Forge rebuild — BEHAVIORS layer locked.** Per audit Q3, lifted `_hoverRafId` + `_hoverPendingId` from closure-local onto `local` so destroy() + rebuildForMode can cancel symmetrically. Added 3 cancel helpers. **(B1)** destroy() cancels all 4 rAF ids (anim/hover/idleLabel/scrub) — load-bearing instead of belt-and-braces. **(B2)** rebuildForMode calls cancelHoverCoalesce at top — kills the ghost-hover-on-mode-switch class. **(B4)** scrubber rAF-coalesce — refreshUI synchronous, recomputeFocus deferred; same pattern as setHoverId. **(B5)** 22-line FADE-PIPELINE INVARIANT — EXCEPTION SITE comment block above the wholesale-replace site in rebuildForMode. **(B6)** Dim-model dispatcher with A4 default + `_forgeDebug.setDimModel('AX')` + `compareDimModels()`. A1 halves IDLE-hover dim; A3 precomputes per-node ring distance via BFS into `local.scratch.ringDist + fadeDelay`, consumed by tickNodeFades for staggered cascade. **(B10)** 64-line BEHAVIORS section appended to the spec-lock header at top of forge.js — three-state model, hover/click transitions, fade pipeline + exception pointer, rAF ownership map, rebuildForMode ORDER. **New _forgeDebug**: setDimModel, compareDimModels, rafIds. Verified live: deities boots clean, A4 default, dim-model cycling works, A3 BFS verified (r0:1 r1:15 r2:39 beyond:621 = 676 ✓), click-empty regression passes, no rAF leaks at rest, zero console errors. Cache-bust `20260520-rebuild-phase-2b`. Files: `src/js/views/forge.js`, `index.html`. Next: Phase 3A — WIRES micro-audit.
- `opus-forge-rebuild-phase-1b-2026-05-20` (2026-05-20) — **Phase 1B of the layered Forge rebuild — NODE atom locked.** Implementation batch for the 5 required + N6/N7/N8 polish items from [`AUDIT/forge-rebuild-1A-node-atom-2026-05-20.md`](../AUDIT/forge-rebuild-1A-node-atom-2026-05-20.md). **(N1)** hit-test spatial grid in forge.js — uniform grid over worldExtent with cellSize=2×maxRadius, queried 3×3 in hitTestAt; verified correct via world→screen round-trip on hitNode #100 ("saklas"). **(N2)** `local.nodeInstancesDirty` flag plumbed through drawFrame; renderer (`webgpu.js`) gates `nodeInstanceVbo` `writeBuffer` on `dirty || grew`; verified 1 write at mount + 0 over 500ms idle (~106 MB/s saved at 10k). **(N3)** `owned[]` resource list pattern in `webgpu.js` `create()` (own/disown helpers); every eager + lazy GPU resource registered; symmetric `destroy()` iterates owned; verified 12 active GPU resources tracked at mount; closes T2.6 (atlasTex / glyphInstanceVbo / glyphUvUbo silent leaks). **(N4)** `local.packedAtScale = camera.state.scale;` immediately after `packNodes` in rebuildForMode; eliminates latent pack-scale invariant footgun. **(N5)** CROSS-PIPELINE INVARIANT comment block at the `nodeStateVbo` write site documenting glyph-pass reuse contract. **(NODE spec lock)** 30-line header block above PARAM_DEFAULTS in forge.js documenting tier classification, radii, screen-px clamp, state/selected channels, depth z, GPU layout, lifecycle invariant + rebuildForMode ORDER. **(N6)** `packNodes` now returns `tierFor`; callers (`rebuildForMode` + `rebakeNodes`) reuse instead of rebuilding (2 fewer O(N log N) sorts per mode rebuild). **(N7/N8)** comments at `_colorCache` + `TIER_RADIUS` fallback. **New `_forgeDebug` API**: nodeOnly toggle, dumpHitGrid, dumpPackedAtScale, countNodeVboWrites, ownedCount. **Verified live**: deities mode boots clean, FRAME 0.1 ms, 676 nodes / 3033 edges, zero console errors, no visual regression. Cache-bust `20260520-rebuild-phase-1b`. Files: `src/js/views/forge.js`, `src/js/engine/renderer/webgpu.js`, `src/js/engine/graph/node.js`, `index.html`. Next: Phase 2A — BEHAVIORS micro-audit.
- `opus-forge-rebuild-phase-0-2026-05-20` (2026-05-20) — **Phase 0 of the layered Forge rebuild — dev panel DELETED entirely.** After John called a foundational reset (*"at this stage is a mess, audit the component ground up — node, behaviors, wires, fx, management"* + *"remove the panel completely and REDO later if needed"*), dispatched 5 parallel read-only audit goblins covering scale, state-ownership, lifecycle, animation, and GPU pipelines. Synthesized into [`AUDIT/forge-robustness-lock-plan-2026-05-20.md`](../AUDIT/forge-robustness-lock-plan-2026-05-20.md) and the layered execution spec at [`AUDIT/forge-rebuild-layered-spec-2026-05-20.md`](../AUDIT/forge-rebuild-layered-spec-2026-05-20.md). Phase 0 shipped here: deleted `src/js/engine/dev-panel-forge.js` (670 lines), the dev-panel + icon-library `<script>` tags in `index.html`, the `#forge-dev-tab` + `#forge-dev-panel` + `.fdp-*` CSS block in `src/styles/app.css` (~454 lines), the Option-B pull + `setParam`/`setIcon`/`setFont` + `window._forge.*` bridge exports in `src/js/views/forge.js` (~155 lines), plus comment cleanups in `webgpu.js` + `contract.js`. **Net**: forge.js 2361→2206 lines, app.css 6419→5935 lines, dev-panel file gone = ~1300 lines of drift surface eliminated. PARAM_DEFAULTS in forge.js is now the single source of truth for visual parameters. Verified live in preview iframe: Forge boots clean (676 nodes / 3033 edges / FRAME 0.1 ms, zero console errors). Cache-bust `20260520-rebuild-phase-0`.
- `opus-forge-edge-perimeter-fade` (2026-05-19) — four polish items in one commit. **(1)** Removed the redundant `FORGE` status-tag in the canvas status strip; the app shell's `#view-title` already says "Forge" in the top header. **(2) Edge endpoints at disk perimeter** — `packEdges` now accepts an optional `nodeRadii: Map<id, r>` and offsets each wire's source/target endpoint to the disk perimeter (along the source→target direction) instead of leaving both at the node center. View layer builds the radii map from the packed node-radius column after `packNodes` runs. Different wires out of the same hub now emerge from different points on its circumference instead of bundling at one spot. `rebakeNodes` (zoom-aware re-pack) now also calls `rebakeEdges` so endpoints follow node radii through zoom. **(3) Node fade animation** — `local.nodeStates` is now the live-animating buffer (was reassigned wholesale); new `local.nodeTargets` holds the snap-to value; new `tickNodeFades` advances toward target at FADE_DURATION (same 0.25s as edges). Dim and selected both ease in/out — selected-glow grows smoothly instead of popping. **(4) Label fade** — `.forge-label` uses opacity transition (0.25s ease-out) instead of display:none/block. `syncLabels` sets `data-visible="1"` attribute; CSS rule fades. Labels leaving the visible set fade out instead of vanishing. Glyphs also got `transition: opacity 0.25s ease-out` so their occlusion fade is smooth. **Plus**: filed the core engineer's the portable core sibling-product handoff at `00_meta/the portable core-HANDOFF-2026-05-19.md` per its own instructions (not urgent, trigger conditions: design lock + protection-phase request — none active). Memory pointer added. Cache-bust `20260519-edge-perimeter-fade` (16 occurrences). Files: `src/js/engine/graph/edge.js`, `src/js/views/forge.js`, `src/styles/app.css`, `index.html`, `00_meta/the portable core-HANDOFF-2026-05-19.md` (new).
- `opus-forge-glyph-occlusion` (2026-05-19) — surgical fix for John's "symbols are going over highlighted nodes' circles, no hierarchy coherent" report. Root cause: glyphs are DOM SVG spans positioned over the canvas. Canvas-side disks have proper z-layering (selected z=0 wins over neighbor z=0.3/0.6 via depth test), but DOM glyphs sit on top of the entire canvas regardless of which canvas-side disk they geometrically overlap — so a focused-neighbor's glyph visually paints over the selected node's glow halo. **Surgical fix shipped here**: `syncGlyphFocus` now computes an OCCLUSION ZONE (screen circle = `n.r * scale * glow_extent`) around each SELECTED node; any non-selected glyph whose screen center falls inside any occlusion zone gets `opacity: 0`, so the selected halo stays visually uncluttered. Also wired `syncGlyphFocus` into `camera.onChange` so occlusion updates live as the user pans/zooms past selected nodes. **Deferred (architectural)**: move glyphs into the WebGPU canvas (texture atlas of 17 glyph SVGs + per-instance shader pass with same z-layering as disks). The right long-term fix — glyphs would participate in the same depth pipeline as disks, no DOM/canvas seam — but ~2hr batch by itself. Cache-bust `20260519-glyph-occlusion`. Files: `src/js/views/forge.js`, `index.html`.
- `opus-forge-glow-glyph-fix` (2026-05-19) — two follow-up polish fixes for the items deferred from `03e1589`. **(1) Selected-node glow clipping square** — vertex shader now uses `quad_scale = v.selected_glow.w * 1.5` (was equal to `selected_glow.w`). The fragment shader's smoothstep still has the same `(1.0, glow_outer)` falloff range, but the QUAD now extends 1.5× past `glow_outer`. Pixels beyond the smoothstep range return `glow_fade = 0`, `final_a < 0.08` discard threshold (bumped from 0.04), no depth write — adjacent disk quads paint cleanly through where the selected node's glow has fully dissipated. The visible square outline around selected-node glows is gone; falloff is perfectly circular. **(2) Glyph z-order by state** — `syncGlyphFocus` now sets `z-index: 3 / 2 / 1` per glyph based on `selectedSet` / `focusedSet` / dim. A selected node's glyph always paints on top of its neighbors' glyphs (was DOM-order-dependent and could be buried). Files: `src/js/engine/renderer/webgpu.js`, `src/js/views/forge.js`, `index.html` (cache-bust `20260519-glow-glyph-fix`).
- `opus-forge-resize-snap-fix` (2026-05-19) — found the real cause of John's "wheel snaps and centers ~1 second after resize" report. **Root cause**: `src/js/app.js:10267` has a window.resize handler that debounces 200ms then calls `setView(STATE.view)` — for Forge that means a full teardown + re-mount via `VIEWS.forge.render()` → engine destroyed → fresh `setup()` → `resizeAndFit(true)` → fitToExtent. Forge has its own ResizeObserver that already handles canvas resize without a re-mount; the global app.js handler is for views that read clientWidth at mount and don't have their own observer. Fix: app.js window.resize handler now short-circuits if `STATE.view === 'forge'`. Also added **camera pan bounds**: new `camera.setPanBounds(x0, y0, x1, y1)` API; view layer calls it after every `fitToExtent` with a half-worldSpan margin so the user can pan a half-viewport beyond each edge but not infinite-pan into empty space. Camera `panByScreen` + `tick` both clamp the center. Files: `src/js/app.js` (forge skip in resize handler), `src/js/engine/camera.js` (pan bounds API), `src/js/views/forge.js` (calls setPanBounds after fitToExtent), `index.html` (cache-bust `20260519-resize-snap-fix`).
- `opus-forge-fade-gradient-v2` (2026-05-19) — three follow-ups on John's first-look feedback. **(1) Resize stops re-fitting** — removed `camera.fitToExtent` from `resizeAndFit` sizeChanged branch. The camera now preserves the user's zoom + pan state through a viewport resize; only the canvas pixel size changes, world coords stay anchored. `rebakeNodes()` still runs (screen-px clamp depends on `camera.state.scale` which IS preserved). **(2) Gradient cranked from `mix(1.0, 0.55, t)` → `mix(1.0, 0.25, t)`** — 45% darken at target end was imperceptible on faint idle wires; 75% darken makes the direction unmissable. **(3) Fade bumped from `FADE_DURATION = 0.10s` → `0.25s`** — John reported "no fade in on over"; 100ms apparently passed before perception registered. 250ms is the upper bound of "still snappy". Cache-bust `20260519-fade-gradient-v2`. Files: `src/js/engine/renderer/webgpu.js`, `src/js/views/forge.js`, `index.html`.
- `opus-forge-fade-gradient` (2026-05-19) — two visual upgrades. **(1) Hover/lock fade** — `local.edgeStates` is now the live-animating buffer; `recomputeFocus` writes the snap-to value to a new `local.edgeTargets` and kicks the rAF loop. `tickEdgeFades(dt)` in `animTick` advances states toward targets at `FADE_DURATION = 0.1s`. Hover-in feels snappy; hover-out lingers just long enough to register. `rebakeEdges` (dev-panel param change) was also reworked to update targets instead of snap-replacing states, so a mid-fade param change doesn't kill the animation. **(2) Universal wire gradient** — every wire (idle AND active, all 7 buckets) now darkens from source endpoint toward target endpoint. New `edge_t` varying in the WGSL VsOut carries the bezier `t` parameter (0 at source, 1 at target) to the fragment shader, which multiplies color.rgb by `mix(1.0, 0.55, edge_t)` — 45% darker at the target end. Provides eye-readable direction cue + helps visually separate overlapping wires sharing endpoints. dumpBugState gained `edgeTargets` + `animRafActive` fields for future fade-debug pastes. Files: `src/js/engine/renderer/webgpu.js`, `src/js/views/forge.js`, `index.html` (cache-bust `20260519-fade-gradient`). Note: preview iframe throttles rAF so the fade was not directly verifiable in the harness; gradient verified visually + bake-state + pack-scale-fix all still green.
- `opus-forge-pack-scale-fix` (2026-05-19) — **fixed the "tiny dots on load + after resize" bug class** that survived all prior Option B / convention-flip / cache-bust attempts. Root cause: `packNodes` bakes world radius from `camScale` at pack time (screen-px clamp in `node.js:126-131`), but `rebuildForMode` was packing BEFORE `fitToExtent` (so first pack used scale=1.0, then camera shrank to ~0.4 → wrong radii baked) AND `resizeAndFit` never called `rebakeNodes()` on resize (only `rebakeEdges()` + `recomputeFocus()` — so cached wrong-scale radii survived every resize). The `camera.onChange` 5%-drift guard did the corrective rebake whenever a user gesture happened to cross the threshold, which is why John saw "snap to correct on first cursor move" — coincidence of the threshold, not by design. Two surgical edits: (1) `rebuildForMode` now computes `ext` + calls `fitToExtent` BEFORE `packNodes`, so the pack runs at the correct scale from frame zero; (2) `resizeAndFit` sizeChanged branch now calls `rebakeNodes()` alongside `rebakeEdges()` / `recomputeFocus()`. Files: `src/js/views/forge.js`, `index.html` (cache-bust `20260519-pack-scale-fix`).
- `opus-forge-bake-step02-plus-optionB` (2026-05-18 night) — baked John's step-02 dev-panel JSON into Forge `PARAM_DEFAULTS` + shipped Option B structural fix (engine pulls dev-panel state via `window.AtlasEngineForgeDevPanel.getState()` on view mount, eliminating the panel-vs-engine drift bug class permanently) + fixed dual-defaults architectural bug discovered during verification (exposed `window._forge.PARAM_DEFAULTS` as single source of truth; `dev-panel-forge.js` hydration now prefers engine defaults over per-control fallback). Verified live in preview iframe: all bake values landing (active_color_transmission #5a4bd5, dim_amount 0.80, wire_min 1 / wire_max 2, label thresholds 0.10/1.20/1.65/1.95, idle slate palette), `forge_PARAM_DEFAULTS_exposed: true`, GPU + JS edge states both all-zero (idle convention flip still holding). Files: `src/js/views/forge.js`, `src/js/engine/dev-panel-forge.js`, `index.html` (cache-bust `20260518-bake-step02-optB`).
- `opus-forge-edge-state-readback` (2026-05-18 PM) — added GPU-side readback probe to break the speculation loop on the recurring "wires light up after resize" bug. Renderer now exposes `debugReadEdgeStates()` + `debugReadNodeStates()` (async, MAP_READ staging buffer). View exposes one-shot `_forgeDebug.dumpBugState()` that combines JS-side state + GPU-side readback + bucket palette + camera/viewport/DPR/cache-bust in a single JSON dump. Ground truth for John to paste back when the bug appears. `ensureBuffer` now adds `COPY_SRC` to all state/instance VBOs so the readback methods are valid.
- `opus-forge-cachebust-bump` (2026-05-18 PM) — bumped cache-bust slugs in `index.html` to force fresh JS fetch.

### Recently released
- `opus-forge-cachebust-bump` (2026-05-18 PM) — bumped 17 cache-bust slugs in `index.html` from `20260518-forge-edge-state-invariant` → `20260518-cachebust-bump` so John's Brave fetches fresh JS on reload. Diagnostic step for the resize-hot-wires issue per `AUDIT/forge-edge-state-invariant-2026-05-18.md` addendum (H1: cached old JS).

## Current rollout queue (UX agent picks the next one)

Priority order for the kit + view migration sequence (from the Workflow Reset plan):

1. **Phase 3 — Kit extraction** — Lift Pantheon V2 design primitives into `src/js/kit/`. Pantheon V2 keeps rendering identically. *(Not started.)*
2. **Phase 4 — Documents view migration** — Adopt kit primitives in `src/js/app.js` Documents view. *(Blocked on Phase 3.)*
3. **Transmission view migration** — Biggest visual win after Documents.
4. **Atlas (map) view** — Adopt `thumb-card.js` for node hover.
5. **Timeline view** — Adopt tier colors + phase-band tokens.
6. **Astrology spine + wheel** — Adopt `rim-labels.js` + `force-bake.js`.
7. **Scripture ring** — Adopt `edge-buckets.js` for cross-book trails.
8. **Alphabets glyph viewer** — Adopt `thumb-card.js` + tokens.
9. **Remaining list views** — Authors, Themes, Edges, Traditions, All-nodes — token-only normalization.

## How to claim (3 lines)

1. Confirm slot is open (table above empty). If not, wait or do investigation work.
2. Fill the slot row: `| your-handle | what you're doing | the exact files you'll touch | YYYY-MM-DD HH:MM | started |`. Path enumeration must be explicit — "Owns: src/js/" is not specific enough.
3. When you commit and verify via `mcp__Claude_Preview__`, release the slot (move row to "Recently completed" or delete) and add a one-line entry to the top of `00_meta/STATUS.md`.

## Recently completed (last 5)

- `opus-forge-phase-0` (2026-05-17) — **Forge tab scaffold + Craft doctrine added to AGENTS.md.** New isolated route (`?view=forge`) with the same isolation pattern Pantheon V2 used: own pane, own state, zero shared mutable state. New `src/js/engine/` folder with `contract.js` (Scene/Camera/Renderer API), `types.js` (Style / Gradient / Transform / BlendMode, hand-mirrored from the portable core's `types.ts` which mirrors the portable core Rust), `math.js` (affine transforms / world↔screen / bounds / bezier / color helpers, ported from the portable core's `canvasGeometry.ts`), and a `README.md` defining architecture invariants. The Forge view module (`src/js/views/forge.js`) is Phase 0 only — renders an informational pane reporting engine status (modules loaded, WebGPU available, data visible) + the phase roadmap. No GPU device yet; that's Phase 1. AGENTS.md now carries the new **Craft doctrine**: (1) we are NOT shipping until 100 % done — no "ship fast" posture; (2) re-evaluate tech at every friction signal — standing obligation to escalate wrong-tech findings; (3) proprietary, not rented — no SaaS / commercial deps for the production surface; (4) forward compatibility — engine contracts mirror the portable core so future Rust+WASM port stays mechanical. Verified live: Forge tab present in side nav with "p0" badge, all 3 engine modules report loaded, WebGPU available, 3110 nodes / 663 deities / 19043 edges visible, Pantheon V2 still mounts cleanly on view-change. Files: `AGENTS.md`, `src/js/engine/contract.js` (new), `src/js/engine/types.js` (new), `src/js/engine/math.js` (new), `src/js/engine/README.md` (new), `src/js/views/forge.js` (new), `src/js/app.js`, `src/styles/app.css`, `index.html` (cache-bust `20260517-forge-p0`).
- `opus-edge-buckets-universal-1` (2026-05-17) — Universal 7-bucket edge palette + ONTOLOGY-correct routing. New `src/js/edge-buckets.js` is the single source of truth; consumed by both `pantheon-v2.js` and `app.js` via `window.edgeStyleFor()`. The `syncretic` family (935 + 792 + 336 + 22 = ~2,085 edges) moved from Parallel → Fusion per ONTOLOGY.md §3 — Inanna ↔ Ishtar / Poseidon ↔ Neptune-Roman / Isis-Hellenistic identifications now paint amber, not teal. `heir-of` + `descended-from` + `syncretic-direct-influence` now route to Transmission terracotta. Headline opacities restored to AUDIT spec (Polemic 0.25, Fusion 0.30, `ancestor-of` 0.30). Dormant legacy `EDGE_STYLE` deleted from `pantheon-v2.js`; `app.js` keeps `EDGE_STYLE[t]` as a Proxy delegating to the universal map for back-compat. Verified live: 1,977 fusion edges rendered in V2 Deities mode. Files: `src/js/edge-buckets.js` (new), `src/js/views/pantheon-v2.js`, `src/js/app.js`, `index.html` (cache-bust `20260517-edge-buckets-universal`).
- `opus-pantheon-focus-vis-1` (2026-05-17) — Focus-state visual overhaul + dev-panel always-on. (1) Removed the `?dev=1` URL gate from `src/js/dev-panel.js`; the vertical "Dev Panel" tab now appears on every page load, `D` key still toggles. (2) Type-glyphs no longer paint stark white — each glyph picks up a lighter hue of its node's family color via a new `lightenColor(hex, 0.55)` helper, set inline as `style.color` at build time; `currentColor` inside the SVG paths inherits the tint. The dev-panel `Type glyph opacity` slider now controls actual `opacity` (not color alpha). (3) New `applyTypeGlyphDim()` toggles `.ph2-type-glyph-dim` (opacity 0.10) on entries whose node is in the DIM state or whose family is filtered out — wired into both `applyEdgeHoverState` and `applyHullFilterState`. (4) Split the SVG overlay into two: hulls + ticks stay below sigma (so disks paint on top of family backgrounds), edges are lifted into a new `.ph2-edges-overlay` at `z-index: 2` so the bright wires paint OVER the dim disks when a node is locked. Verified live: clicking Zeus dims every non-focused disk to a soft family hue at 10% opacity, the radial wires to Jupiter/Apollo/Marduk/Baal-Hadad/Indra read clearly across faded clusters, and the dev tab is visible without any URL flag. Files: `src/js/views/pantheon-v2.js`, `src/js/dev-panel.js`, `src/styles/app.css`, `index.html` (cache-bust `20260517-focusvis`).
- `opus-pantheon-polish-1` (2026-05-17) — Three Pantheon V2 polish fixes. (1) Music type-glyph redrawn as a clean quarter-note (head + stem, matches the toolbar's `♩`) — replaced the previous double-eighth design which read as noise at small sizes. (2) Smart-label space-fill rule: in `hub` label mode, every label now enters the deconflict pool — hubs claim their slots first (highest priority by degree), then lower-tier labels are promoted to visible when their bbox doesn't collide with any already-claimed bbox. Empty-hull traditions (Christian / Macedonian / Pre-Islamic Arabian / Pacific…) now surface dozens of secondary labels. (3) Node tier-radii bumped from `[13,10,7,5]` → `[16,12,9,7]` — the rest-tier (low-degree single nodes in sparse hulls) is now 40 % bigger and clearly legible. Files: `src/js/views/pantheon-v2.js`, `index.html` (cache-bust `20260517-polish`).
- `opus-pantheon-hashrouter-1` (2026-05-17) — Pantheon V2 hash router + dev-panel knobs. URL params `view` / `mode` / `families` / `locked` / `focus` round-trip cleanly: mode change, family-filter, hull-toggle, node-click, and empty-stage-reset all update the URL via `replaceState`; cross-view navigation (Pantheon → Timeline → Pantheon) uses `pushState` so the back-stack moves between views; deep-links like `?view=pantheon&mode=documents&focus=phase-3-021-hermetic-corpus-earliest` mount the wheel in that exact state. Dev panel got two new sections — `Hover card` (card width / image height / tagline line-clamp) and `Type glyphs` (wheel-glyph scale / opacity) — gated on `?dev=1`. Files: `src/js/app.js`, `src/js/views/pantheon-v2.js`, `src/js/dev-panel.js`, `src/styles/app.css`, `index.html`.
- `opus-pantheon-hovercard-1` (2026-05-17) — Pantheon V2 hover-card rebuild + per-node-type SVG glyph vocabulary. Wide cover-cropped image, curated `role` tagline (falls back to attributed Wikipedia extract — no invented prose), family · BCE/CE period meta, edge-bucket connection breakdown, tag-label chips. New `ph2-type-glyphs-layer` paints a distinct geometric primitive per node type (deity / author / document / symbol / event / ritual / music / alphabet / alchemy / philosophy / moral / medicine / mathematics / monument / theme / tradition / place) — first-pass primitives, slot in hand-illustrated vectors by replacing TYPE_GLYPHS entries. Files: `src/js/views/pantheon-v2.js`, `src/styles/app.css`, `index.html`.

_Older UX work is in `agents-archive/2026-05-W2-active.md`._
