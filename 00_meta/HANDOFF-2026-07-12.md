# HANDOFF — Codex Atlas, 2026-07-12 (session end — account swap for credits)

**Self-contained pickup for a fresh agent (new account).** Read this + the cardinal memories.
**Tree is CLEAN** at HEAD `5b7394c4` (only untracked = John's `CLAUDE.md` — pre-existing, **leave it**).
Dev server stopped; start per `.claude/launch.json` (`atlas`, port 8742) when needed.

## Where the vault is (verified 2026-07-12, session end)
- **edges 26,829 → 27,033 (+204) · dead-link floor 338 (held EXACTLY through all 12 commits) · lint 0 · +11 nodes**
- **READ headline is now docs-covered: 234/622 (37%)** — texts-staged (386) is the secondary stat.
  The old "384/621 ≈62%" framing was a units bug (texts vs docs), fixed in the auditor this session.
- **12 gated commits** `abbca16f..5b7394c4`, every one build-0 / floor-338 / lint-0.

## What this session did (the arc)
John's directives: *"audit + design ideas for Boards + best path to ingestion for transmissions to
modernity"* → *"go"* → *"make it ROCK solid"* → *"use efficient agents, you just audit."*

1. **AUDIT** (`AUDIT/2026-07-12-status-audit-modernity-ingestion-boards-design.md` — READ THIS, it is
   the session's map): era census (86% of dated nodes pre-1500, 8 nodes post-2000, 444/573 modern
   nodes wire-dead), the verified missing-keystone list, **the keystone-relay ingestion plan (5
   phases)**, 8 Boards feature proposals (Trace-to-Modernity, time-axis snap, frontier markers…).
2. **Integrity fixes (Lane B `ba98c838`):** 6 broken docNode reader→graph bridges repointed; coverage
   auditor now prints docs-covered/622 as headline + hard-fails unresolvable docNodes.
3. **Boards housekeeping (Lane B ×4):** legacy alchemy board SUNSET to `_legacy/` with all callsites +
   dead CSS purged same-commit (−439 lines); `tr-` id namespace fixed IN THE GENERATOR; 927 lines of
   AI-preset data moved to `src/data/boards-presets.js` (new file; boards-library.js is GENERATED —
   never hand-edit it); Save-tree comment corrected. All screenshot-verified, 0 console errors.
4. **KEYSTONE PHASE 1 (`cc2731a0`):** Golden Dawn cluster — `tradition-hermetic-order-golden-dawn`
   (born deg-30; Sprengel forgery documented AS fabrication per Howe 1972) + Mathers + Waite + Papus,
   20 reciprocal wires. Marseille-tarot de-orphaned.
5. **KEYSTONE PHASE 2 (`4238534e` + `79d69212`):** psychology + monomyth relay — `theme-monomyth`
   (framed as a 20th-c construct WITH its critique; generalization-over wires, never ancestry) +
   Tylor + Watkins + Puhvel (the 3 missing comparativists from HANDOFF-2026-07-06 #3 = DONE);
   Freud's first edge fields; Jung ← Freud/Gnosticism. The skeptic caught **Puhvel's death
   (25 Apr 2026, post-cutoff, web-verified)** — trust the skeptics on dates/citations.
6. **KEYSTONE PHASE 3 (`e412daa5` Lane A + `5b7394c4` Lane B):** 19th-c connective tissue —
   `tradition-mesmerism` + `alan-watts` + `timothy-leary` + a 28-wire develop pass that took
   `tradition-spiritualism` 0→9 wires (root cause: its links sat in INERT fields `sister-traditions`/
   `parent-tradition` — see the registered-field memory). READ batch 32: Swedenborg *Heaven and Hell*
   (Ager 1900) + *Isis Unveiled* (1877), both skeptic-verified character-exact vs PG downloads.
   Chains now run **Paracelsus→Mesmer→Spiritualism→Theosophy→New Age→Watts/Leary** and
   **Bardo Thödol→Leary (1964)**.

## The method that worked (KEEP IT — John ratified "efficient agents, main thread audits")
Three engines in parallel, main thread gates only:
- **`atlas-ingest-cluster`** (named workflow) for CREATES — records pre-verified absent (grep whole
  vault + canonical-slugs.md first). All 10 drafts this session came back "revise" with real catches.
- **`atlas-read-stage`** for READ texts — PD verified per text; **the 1850–1930 esoteric layer is a
  PD GOLDMINE** (confirmed in practice), the escape from the ANE public-domain wall.
- **A read-only general-purpose "develop-spec" agent** for wiring EXISTING starved nodes — returns
  a JSON wire-spec with per-wire citations + target verification; main thread spot-checks then applies.
Gates every batch (non-negotiable): skeptic-fix application → wikilink sweep across ALL 31 dirs →
LINE-SURGERY appends (**never regex-append**: memory `feedback-yaml-inline-list-append-trap-2026-07-12`;
corruption grep `\[\[[a-z-]*, "\[\[` before gating) → build/linkcheck-338/lint → separate Lane A/B commits.

**Skeptic quirk (hit 3×/3 batches):** skeptics flag "TRUNCATION mid-word/mid-sentence" but they review
an EXCERPT — before treating as blocking, `tail`/grep the actual draft file; all 6 such flags this
session were artifacts. Their citation/date/factual catches, by contrast, were all real.
**Read-stage quirk:** 1 of 3 drafts (james-varieties) died to content filtering — per memory
`feedback_delegated_agent_content_filter`: retake in the MAIN THREAD in small encyclopedic chunks.

## Next steps (ranked — fresh)
1. **KEYSTONE PHASE 4 — the reception terminus** (the audit §2 phase 4): CREATE a small
   pop-culture-reception cluster (Star Wars/monomyth-cinema — `theme-monomyth` is wired and waiting,
   its body already cites the Lucas–Campbell link; Tolkien/modern fantasy; comics-as-mythology) +
   `robert-graves` + `manly-p-hall` (*Secret Teachings* 1928 = PD, READ-stageable). All verified
   absent 2026-07-12. This is what makes every chain end at a name users recognize.
2. **KEYSTONE PHASE 5 — lived religion + scholar spine:** DEVELOP `tradition-pentecostalism`
   (EXISTS at deg7 — INVESTIGATION-LEADS §V wrongly lists it as missing) toward global-South
   Christianity; wire the orphaned scholars (`marija-gimbutas` 1, `james-hillman` 0, Eliade 11) into
   `tradition-comparative-religion-academic` (23).
3. **Aldous Huxley stub→full body** — the develop-spec agent left a complete cited outline; it is in
   the phase-3 spec JSON at the session scratchpad (gone after swap) BUT summarized: Bedford 1973–74 +
   Murray 2002 (life), Sawyer 2002 (Vedanta period), Schmitt JHI 1966 (perennial-philosophy lineage),
   Dunaway 1989 + Smith 2000 (psychedelic turn), Zaehner 1957 + Katz 1978 (critique). Wires already live.
4. **James *Varieties* READ retake** (main-thread, per the content-filter rule) — docNode
   `phase-7-041-varieties-of-religious-experience`; mysticism lectures XVI–XVII (four marks); 1902 PD.
5. **Reader catalog fix (Lane B):** `scripture-reader.js` CATALOG hand-maps only 14 of ~385 texts —
   everything else piles into "Other/Uncategorized" in the pickers. Two-sources-of-truth (rule #10
   Pattern A): derive religion/group from each entry's `tradId`/`corpus`, keep CATALOG as override.
6. **More PD-goldmine READ batches:** *Pictorial Key to the Tarot* (Waite 1910), *Kabbalah Unveiled*
   (Mathers 1887), Lévi–Waite *Transcendental Magic* (1896), *The Kybalion* (1908), *Book of the Law*
   (Crowley, PD), Frazer chapters — the uncovered phase-6/7 list is in the auditor output.
7. **Boards feature work (Lane B, needs John's pick):** the audit §3 ranked 8 proposals; John has not
   yet chosen. Do NOT start without his pick (it is his product-design call).
8. **Still open from before:** Ashoka duplicate merge (`ashoka` + `asoka-maurya`); the missingTargets
   list from the phase-3 spec (in `AUDIT/…2026-07-12…` addendum below) — top demand:
   `andrew-jackson-davis` (the Swedenborg→Spiritualism hop), `alice-bailey` (Theosophy→New Age bridge,
   double demand), `franz-mesmer` person node, `humphry-osmond`, `huston-smith`, doc nodes for the
   Huxley/Kardec/Davis/Doyle primary texts.

## Key reads & memories
- **`AUDIT/2026-07-12-status-audit-modernity-ingestion-boards-design.md`** — the plan everything
  executes against. §2 = ingestion phases, §3 = Boards proposals.
- `00_meta/STATUS.md` — top ~8 entries = this session (newest first).
- Memories (auto-loaded; same machine user, so they SURVIVE the account swap):
  `feedback-yaml-inline-list-append-trap-2026-07-12` (NEW — read before any scripted wiring),
  `reference-read-coverage-worklist-2026-07-07`, `project-ingestion-master-plan-2026-06-14`,
  `reference-edge-fields-registered-vs-inert-2026-07-05`, `feedback-membership-vs-wire-crisis-2026-06-02`.
- Prior handoffs (still-valid deeper context): `00_meta/HANDOFF-2026-07-08.md`, `HANDOFF-2026-07-06.md`.

## John's working style (match it)
Plain human talk, lead with substance, designer language for technical things. He checks by **sample,
not per-note** — a reader/board screenshot each batch or two. **Execute-don't-menu** within the
ratified framework; ask only at genuine forks (e.g. which Boards feature to build). Agents author,
main thread audits — he ratified this explicitly this session ("use efficient agents … you just
audit to save tokens"). Every batch: STATUS entry + gated commit; no silent finishes. Git author =
`Codex Atlas` pseudonym (Anonymity Law — never John's name).
