# HANDOFF — Codex Atlas, 2026-07-17 (alpha is LIVE online)

**Self-contained pickup for a fresh agent.** Read this + the cardinal memories.
**Tree CLEAN** (only untracked = John's `CLAUDE.md` — leave it). Dev server: `.claude/launch.json` (`atlas`, port 8742).

## 🔴 READ FIRST — how to work with John (memory `feedback-be-concise-one-step-2026-07-17`)
John is a **designer/CEO, NOT technical**. In chat: **SHORT replies, ONE step at a time**, exact place+paste+button. Walls of text overwhelm him. Put rationale/detail in STATUS + commits + this handoff, NOT the chat. He checks by sample.

## What shipped this session: THE ALPHA IS ONLINE — ON ITS REAL DOMAIN
- **LIVE:** **https://codexatlas.org** (custom domain wired via Cloudflare Pages) + https://codex-atlas.pages.dev. The whole app, **gated behind the "Enter the Codex" sign-in wall** (only allow-listed emails get in). John (`mercury@codexcortex.com`) + first friend (`vv.workstudio@gmail.com`) allow-listed. Works end-to-end (magic-link → profile → cloud-synced boards).
- **Domain:** codexatlas.org registered at **Porkbun** (registration stays there), **DNS moved to Cloudflare** (nameservers casey/jule.ns.cloudflare.com), zone Active, added as a **Pages custom domain** on the `codex-atlas` project → SSL auto-issued. Cloudflare zone ID `aad03222a0e9e7388e0c845aabeb473d`, account ID `fe6653e49e7863ce8339dd7465e96177`.
- **Supabase Auth URLs** must include BOTH `https://codexatlas.org/**` and `https://codex-atlas.pages.dev/**` (+ localhost:8742) as redirect URLs; Site URL = `https://codexatlas.org`.
- **To add an alpha friend:** Supabase → SQL Editor → `insert into allowed_emails (email,note) values ('their@email','friend') on conflict (email) do nothing;`
- **Design 100% implemented** (all 6 mockup surfaces): Threshold · Boards "Sealed" loop · Folio (avatar + faith-inspired badges + in-app theme picker) · 4 themes (Codex/Quantum/Human/Mystic) · narrated nav · sign-in modal.
- **Zero third-party requests at runtime** (d3/marked + all 25 fonts vendored locally).

## The go-online stack (all NEW this session)
- **`src/js/gate.js`** — sign-in wall. Gate ON for real hosts, OFF on localhost (dev stays open). Overrides: `?gate=1` force-on, `localStorage['codex-atlas/gate']='off'`.
- **`src/js/auth.js`** — Supabase magic-link seam (`window._auth`): signInWithEmail, signOut, session, `codex:auth-changed` event.
- **`src/js/signin.js`** — "Enter the Codex" modal (`window._signin`), gate-mode (no dismiss).
- **`src/js/sync.js`** — LOCAL-FIRST ⇄ Supabase. Signed out = 100% local; signed in = profile+boards mirror to the user's rows. Events: codex:auth-changed / codex:profile-changed / boards:library-changed.
- **`src/js/supabase-config.js`** — project URL + PUBLISHABLE key (public, RLS-gated; NEVER service_role).
- **`_assets/vendor/supabase/supabase.js`** — vendored client.
- **`supabase/migrations/0001_auth_foundation.sql`** — profiles + boards + `allowed_emails` (the on/off switch: signup RAISES for non-listed email) + owner-only RLS + auto-provision trigger + sequential Alpha Seal Nº. Mirrors the CODEXCORTEX pattern.

## Infra (John's, pseudonymous — see `PROJECT-IDENTITY.md`, gitignored)
- **Supabase**: org "Codex Atlas" → project `codex-atlas` (eu-central-1). URL `hhfhlzwjzqevvnadlueg.supabase.co`. Login `supabase@codexcortex.com`. Allowlist managed in Table Editor → `allowed_emails`.
- **Cloudflare Pages**: project `codex-atlas` (account `cloudflare@codexcortex.com`, wrangler authed). Prod alias `codex-atlas.pages.dev`.
- **Supabase Auth URL config**: Site URL + redirect = `https://codex-atlas.pages.dev/**`.

## HOW TO DEPLOY (repeat after any change)
```
python3 scripts/build_dist.py                # builds dist/ (Pages-ready)
npx wrangler pages deploy dist --project-name codex-atlas --commit-dirty=true
```
`build_dist.py`: copies app + vendor/bg/audio/data assets; EXCLUDES the source vault, Art Direction, the 177 MB basemap, the 2 HD bg videos, the pmtiles CLI; **splits `data.js` into <25 MB chunks** (Pages caps files at 25 MB — `data-base.js` + `data-nodes-*.js`, reassembled via `.push`, zero app change); sets `CODEX_ALPHA_HIDE_MAP` (hides MAP + STAR MAP — the basemap can't ship).

## 🎯 NEXT SESSION — John's PRIMARY focus: REDESIGN THE DEITIES-CHART NODE
John: *"a node that SELLS, respects the app — a circle that behaves like GLASS with a PRISM effect when hovered, giving [depth/refraction] instead of the current flat circles."*
- The nodes are the little circles on the ATLAS/DEITIES wheel (the Forge chart).
- Rendering lives in the Forge engine: `src/js/engine/renderer/webgpu.js`, `src/js/engine/graph/node.js`, `src/js/engine/graph/glyph.js`. **Cardinal memory `feedback-no-bespoke-graph-renderers`: vault-node graphs = the Forge engine — do NOT build a bespoke DOM renderer.**
- Goal: glassy/3-D nodes with a prism/refraction hover effect. Prototype, get John's eye early (he checks by sample), keep 60fps (memory `feedback-google-maps-bar` + Safari-perf memories — build for Safari).

## Secondary next tasks
- **Declutter the Forge dev panels** (FX / STYLE / VIEW / LEGEND). John: "remove redundant/not-wired, keep what works — I use it for dev." ⚠️ They ARE all wired (LS `forge.viewSettings.v7`, fx-panel.js, style-panel.js, view-settings.js, legend.js) — audit each control carefully before removing; don't break his tools. (Did this session: relabeled the cryptic "Hulls (calm layer)" → "Tradition wedges (colour)".)

## Parked (John: "we'll do those later")
- **Add alpha friends**: `insert into allowed_emails ...` in Supabase.
- ✅ **DONE: `codexatlas.org` is live** (Pages custom domain). REMAINING: **verify WHOIS privacy is ON** at Porkbun for codexatlas.org (was uncertain). Note: Porkbun account is under John's real name (`redacted`) — private registrar login, not public, but a standing anonymity note.
- **Password login** option (John asked for it, alongside magic-link).
- **HD background video** on live (currently the 2.9 MB `bg-t01.mov`; the 27 MB HD versions exceed the 25 MB Pages cap → host on Cloudflare R2 or external if John wants HD).
- **Node imagery** currently loads from Wikimedia at runtime (a third-party call) — bake locally at release (memory `feedback_release_offline_and_snapshots`).

## Key commits (this session)
`ALPHA-A1..A7` hardening+design, `ALPHA-B0..B4` Supabase/auth/gate, `ALPHA-B3a` error, `ALPHA-C1` deploy, `8bbf84fa` menu polish, + this cleanup. All build-verified.
