# HANDOFF — Codex Atlas, 2026-07-06 (session end)

**Self-contained pickup for a fresh agent.** Read this + the cardinal memories +
`AUDIT/2026-07-05-branch-population-benchmark.md`. **Tree is clean** at handoff (HEAD `9854ebe0`);
the only untracked file is `CLAUDE.md` (John's project-instructions file — pre-existing, leave it).

## Where the vault is (verified 2026-07-06, session end)
- **nodes 5,361 · edges 26,795 · dead-link floor 338** (baseline holds) · lint 0 errors
- **READ 356 / 621** texts staged (Lane B, `src/data/scripture-texts.js`) — **parked**, resume anytime
- **24 gated commits** this session `953752bf..9854ebe0` (build 0 · linkcheck no-regression · lint 0 on every one)

## What this session accomplished (the whole arc)
1. **Ranked** the top-10 deities by transmission + **evaluated** all 10 for civilization-lens reach
   (`AUDIT/2026-07-05-top10-roots-coverage-evaluation.md`): Indra/Hermes/Jesus carry the sciences;
   Zeus is a branch-thin spine-hub; Thor/Perun thin; Inanna≈Tiamat, Mary≈Jesus (redundant).
2. **Populated the branches across the lenses AND to a living-modern terminus** (the 5-axis standard):
   Tiamat, Indra, Hermes, Jesus (the 4 powerhouses), plus **Osiris** (real branch) and **Zeus** (honest
   light touch). Each reaches today (Jung/Frazer/Campbell/Locke→human-rights; Vivekananda/yoga;
   Blavatsky/Crowley/New-Age; neo-Thomism).
3. **Cross-branch "massive wins":** panchāṅga↔Babylonian calendar (Metonic); astrology joins
   Tiamat+Indra+Hermes; `wisdom-as-cosmic-order` now spans **6 traditions** (Mesopotamian/Egyptian/
   Greek/Vedic/Zoroastrian/Christian → natural-law → UDHR); the `dying-rising-god` spine; and the
   **`*Perkʷunos` PIE storm-god headwater** (new node) uniting Indra/Thor/Perun/Zeus.
4. **Finished the modern-transmission axis:** the comparative-religion discipline
   (`comparative-religion-method` + `tradition-comparative-religion-academic`) is now the shared modern
   terminus, wired to the ancient clusters it reconstructed; created **`georges-dumezil`**.
5. **Dev panel:** added the **Deity Transmission Ranking heatmap** (`scripts/build_transmission_ranking.py`
   → `src/data/transmission-ranking.json`; ✦ user-menu → Dev). Refreshed wire/health/scripture coverage.
6. **Fixed the inert `hub-edges` bug** — registered it in `build_data.py`; recovered **403** invisible
   sourced wires (0 dead after closing the 2 gaps: created `ka-ba-ye-zardosht`, dropped a phantom tradition).
7. **Resumed scripture READ** — batch 20 (+3): `rig-veda-1-32`, `book-of-the-dead-125-judgment`,
   `rig-veda-8-48`, chosen to surface the wired clusters (storm-god / cosmic-order-as-law / soma-eucharist).

## The standard (codified — apply to EVERY future branch)
A branch is "populated" only when it satisfies **both** axes (benchmark guide §10–§11):
- **all lenses** — wire the civilization lenses (law/phil/math/med/astr/div/cal/mus/alch), not just the spine
- **⏩ reach-to-today** — the chain must terminate in a living/contemporary node (dead-ending pre-~1700 = unfinished)

Method every time: **discover → wire the existing un-wired endpoints → create only genuine gaps →
gate → verify edges live in `data.js`.** Wiring dominates creation (node-rich/wire-poor). RIGOR:
grep-verify every node/textId yourself; never trust an agent's "it exists." Gates, every batch, real
exit codes: `python3 build_data.py` (0) · `python3 linkcheck.py --baseline` (338 floor, no regression) ·
`python3 lint_yaml.py --strict` (0 errors) · `node --check src/data/scripture-texts.js` for READ.

## Two lanes (physically disjoint — see `00_meta/LANES.md`)
- **Lane A — content wiring** (the lens `.md` folders). This session's main work; mature. Commit
  content `.md` separately from `src/` (the pre-commit hook refuses mixing).
- **Lane B — `src/data/scripture-texts.js`** (READ staging) + `src/js` + `build_data.py`. Parked at 356.

## Next steps (ranked — fresh; the earlier list is all DONE)
1. **Scripture READ toward 80%** (Lane B, at 356/621 ≈ 57%). Proven recipe: the `atlas-read-stage`
   workflow, **SMALL batches of 3** (6 trips a rate limit). Full recipe: `AUDIT/2026-06-15-read-scaling-handoff.md`.
   Keep surfacing the wired clusters (dying-rising spine, the Logos chain, natural-law→human-rights, the
   storm-god family). Still un-shipped from an old rate-limit: `athanasius-on-the-incarnation`, `laws-of-manu-1`.
2. **More PIE/comparative headwaters** — the **`*Dyēus` sky-father** reconstruction (parallel to Perkʷunos:
   Dyaus/Zeus/Jupiter/Tyr/Dyeus-pater), and the mother-goddess reconstruction. High investigation value.
3. **Deeper branch runs** — each branch got run-1 + one deeper wire; more endpoints remain (e.g. Hermes'
   European transmitters Newton/Paracelsus/Flamel→the Hermetic anchor; Jesus' sacraments/theology lens).
4. **3 modern comparativists still missing as nodes** (would strengthen the modern terminus): `calvert-watkins`
   (cited in `perkwunos`), `edward-tylor`, and Jaan Puhvel. Create + wire to the discipline + their clusters.

## Key reads & memories
- `00_meta/STATUS.md` (top ~15 entries = this session, newest first)
- `AUDIT/2026-07-05-branch-population-benchmark.md` (the template + the 5-axis standard, §10–§11)
- `AUDIT/2026-07-05-top10-roots-coverage-evaluation.md` (which roots are worth populating)
- Memories: `reference-edge-fields-registered-vs-inert-2026-07-05` (which fields emit edges; **hub-edges is
  now registered**), `project-ingestion-master-plan-2026-06-14`, `feedback-membership-vs-wire-crisis-2026-06-02`,
  `feedback-completeness-is-investigation-not-catalogue-2026-06-05`.
