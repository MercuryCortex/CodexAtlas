# AGENTS.md — 60-second onboarding for any agent landing in Codex Atlas

> **You are an agent newly attached to this vault. Read this file before doing anything else. It compresses the pre-flight protocol, the coordination protocol, and the most important conventions into one screen.** Maintained by `opus-housekeeper-1`. Last updated 2026-05-14.

---

## What this project is, in one paragraph

**Codex Atlas** is a cross-tradition investigation tool. Every named deity, every primary religious document, every key historical figure / event / theme / tradition gets its own markdown node with YAML frontmatter. The goal is to map the connections between them — especially **edges from Christianity back to older Egyptian / Mesopotamian / Mystery / Platonic / Persian / Indic sources** (the user, John, calls these "MASSIVE wins"). **Investigation, not advocacy.** Every claim cites a source. Tiered source-integrity (T1 primary / T2 scholarly / T3 reputable secondary / T4 controversial-but-catalogued).

**Vault root:** `~/Desktop/Codex Atlas/`
**Atlas viewer:** `index.html` (open in browser after running `python3 build_data.py`)
**Live state:** [`00_meta/DASHBOARD.md`](00_meta/DASHBOARD.md) (regenerated by `python3 build_dashboard.py`)

---

## ⚡ Mandatory pre-flight (in order, every session)

1. **Read [`00_meta/DASHBOARD.md`](00_meta/DASHBOARD.md)** — live priority queue, top unstubbed wikilink targets, orphan nodes, quality issues, inventory.
2. **Read [`00_meta/ACTIVE-AGENTS.md`](00_meta/ACTIVE-AGENTS.md)** — the in-flight claims at-a-glance table is at the top. **Do NOT pick a slug another agent owns.**
3. **Read [`00_meta/STATUS.md`](00_meta/STATUS.md)** — recently completed batches; what just landed; what's the headline state.
4. **Read [`00_meta/canonical-slugs.md`](00_meta/canonical-slugs.md)** before naming a new node — slug drift was the #1 source of dead links historically.
5. **Read [`00_meta/methodology.md`](00_meta/methodology.md)** + the relevant `schema-*.md` for the node type you're creating.
6. If you're working in a particular phase of `02_documents/`, read its **`_phase-N-…/_TODO.md`** for local scope.

---

## ⚡ Mandatory coordination protocol

### 🚫 BEFORE you do anything — git rules (added 2026-05-15)

- **Work directly on `main`. Never create a worktree branch.** John has confirmed this twice now after losing work to abandoned worktrees: agents got spawned into `.claude/worktrees/<random-name>/`, made unfinished changes, then the session ended without commit + the worktree was cleaned. **9 worktrees accumulated** across sessions before being audited and removed 2026-05-15.
- **The vault is solo + local-only.** No remote. No PR workflow. Branching only causes friction. Edit on main, commit on main, push nowhere.
- **Exception:** if John explicitly says "try this as an experiment" or "I might not want to keep this," THEN you may use a feature branch (so it's easy to discard). Otherwise: `main`.
- **Commit discipline (added 2026-05-15):** if you make app-code changes, COMMIT before stopping your turn. Uncommitted state has bitten the site twice (`opus-foundation-1`). Half-applied edits accumulating across agents is a forensics nightmare. Commit small + often is better than one big lost commit.

### BEFORE you start (or at the latest, while working)

1. **Pick a handle** in the existing convention: `opus-<wedge>-<n>`. Examples in flight: `opus-hellenic-3`, `opus-ethiopian-4`, `opus-design-1`, `opus-gaps-1`, `opus-housekeeper-1`. Pick the next number for your wedge.
2. **Append a row to the at-a-glance table** at the top of `ACTIVE-AGENTS.md` (Handle | Scope tag | Owns | Started).
3. **Append a full claim block** to `ACTIVE-AGENTS.md` (format documented at the top of that file). Owning slugs MUST be explicit; "I'm doing X stuff" is not a claim.

### DURING your work

- If you're adding nodes, follow the schema in `00_meta/schema-<type>.md`.
- Use `[[wikilinks]]` for every cross-reference; never bare slugs in body text.
- Hunt explicitly for the cross-tradition tracing edges that root later traditions in older ones — these are the **MASSIVE wins** John framed as the prize.
- Cite sources at the tier the methodology requires (≥3 refs for `metadata` status, ≥1 T1 primary).

### WHEN you finish

1. **Update your claim block** with `Status: finished`, the full delivery list (node-by-node with wikilinks), the MASSIVE-win edges added, and an "Open gaps for follow-up agents" section.
2. **Add a one-paragraph entry** to `STATUS.md`'s "Recently completed batches" block.
3. **Re-run** `cd "~/Desktop/Codex Atlas" && python3 build_data.py && python3 build_dashboard.py` so `data.js` + DASHBOARD reflect your work.
4. **Remove your row** from the at-a-glance table and add a one-line summary row to "Last session's finishers" (the next session-close housekeeper batch will archive your full claim block to `00_meta/agents-archive/YYYY-MM-DD.md`).
5. **Commit your work** with a `git commit` — the vault is under local git (no remote) per John's directive. Stage your own edits + the auto-regenerated DASHBOARD/canonical-slugs/dead-links files. Optional: run `python3 lint_yaml.py --strict` first to catch any malformed YAML you wrote.

> An agent who completes work but doesn't log it has done **less than half the job**. The next agent's work queue is the dashboard + your finish-block; if you don't write the finish-block, your edges become invisible to them.

---

## ⛔ DO NOT

- **Do NOT touch slugs another agent currently owns** (check the at-a-glance table at the top of `ACTIVE-AGENTS.md`). If you need an edge into one of their nodes, leave a coordination note in your claim block — they'll incorporate it.
- **Do NOT create a duplicate node for a concept that already has a canonical slug** (check `canonical-slugs.md`). Reuse the canonical form. If the existing slug is wrong, propose a rename in `AUDIT/`, don't just create a competitor.
- **Do NOT collapse syncretic identifications into one node prematurely** (El-Canaanite + El-Hebrew stay separate; Inanna + Ishtar + Astarte + Aphrodite stay separate; the edges between them carry the syncretic claim).
- **Do NOT assert "X is just Y renamed" without a Tier 1–2 source.** Comparative-religion claims are *hypotheses* unless an ancient source explicitly identifies them. Use `parallel-form` (resemblance only) by default; reserve `ancestor-of` for documented transmission with a T1 source.
- **Do NOT hand-edit auto-generated files** (`DASHBOARD.md`, `dead-links.md`, `quality-issues.md`, `orphan-nodes.md`, the per-phase `_TODO.md` lists). Re-run `build_dashboard.py` instead.
- **Do NOT skip the rebuild step.** Other agents' DASHBOARD will be stale and they'll pick the wrong priorities.
- **Do NOT delete sourced material because it conflicts with a newer source.** Add the new source and reconcile in a `Disputes` section.
- **Do NOT use AD/BC; always BCE/CE.** Negative integers in YAML for BCE.
- **Do NOT escape your sandbox** (no `cd ..` outside the vault, no `sudo`, no `rm -rf` on anything you didn't create this session).

---

## 🗺️ Folder map (current — phases 1–8 active)

```
Codex Atlas/
├── README.md                  — project overview
├── AGENTS.md                  — this file (agent quickstart)
├── index.html                 — atlas viewer shell (~129 lines)
├── data.js                    — generated by build_data.py (do not hand-edit)
├── build_data.py              — markdown → atlas data
├── build_dashboard.py         — markdown → DASHBOARD/queues/TODOs
├── linkcheck.py · fetch_thumbnails.py
├── src/
│   ├── styles/app.css         — all CSS
│   └── js/app.js              — all JS (one file; ES-module split deferred)
├── 00_meta/                   — methodology, schemas, registries, dashboards
│   ├── DASHBOARD.md           — auto-generated work queue (read first)
│   ├── ACTIVE-AGENTS.md       — in-flight claims (write your block here)
│   ├── STATUS.md              — recently completed batches
│   ├── HANDOFF.md             — fresh-session pickup notes
│   ├── methodology.md         — SOP, edge-typing, anti-patterns
│   ├── source-integrity.md    — tiered citation policy
│   ├── canonical-slugs.md     — shared vocabulary (check before naming)
│   ├── dead-links.md / quality-issues.md / orphan-nodes.md   — auto-generated
│   ├── schema-{deity,document,event,person,symbol,theme,tradition}.md
│   ├── label-taxonomy.md / locations.md / tradition-vocabulary.yaml
│   └── themes-audit.md / themes-to-create.md / app-usage.md
├── 01_timeline/               — master chronological index
├── 02_documents/              — primary texts, organized by phase
│   ├── _phase-1-ancient-near-east/        (~2600–1500 BCE)
│   ├── _phase-2-axial-age/                (~1500–500 BCE)
│   ├── _phase-3-hellenistic-second-temple/ (~500 BCE–100 CE)
│   ├── _phase-4-late-antiquity/           (1st–7th c. CE)
│   ├── _phase-5-medieval/                 (~600–1500)
│   ├── _phase-6-early-modern/             (~1500–1800)
│   ├── _phase-7-modern/                   (~1800–2000)
│   └── _phase-8-non-mediterranean/        (oral / non-Western canons)
├── 03_deities/                — one note per named god/divine figure
├── 04_persons/                — prophets, scribes, kings, philosophers, redactors, scholars
├── 05_events/                 — historical events and discoveries
├── 06_themes/                 — recurring motifs (flood, dualism, demiurge, etc.)
├── 07_traditions/             — overview per tradition
├── 08_refs/                   — catalog of sources (currently empty)
├── 09_symbols/                — symbology layer (per AUDIT/06 methodology)
├── 99_ingest/                 — raw drops (images, screenshots, unprocessed PDFs)
├── _assets/                   — atlas-app static assets (thumbs cache, screenshots)
├── _graph/                    — cross-influence edge lists, visual exports
├── AUDIT/                     — standing audits and proposals (00 = index)
└── .obsidian/                 — Obsidian config (vault opens cleanly)
```

---

## 📜 Status legend (per node, in YAML `status:` field)

- `stub` — title + date placeholder only
- `metadata` — full YAML + ≥1 context paragraph + ≥3 refs (≥1 T1)
- `partial` — selected excerpts/sections of the source text
- `full-text` — complete public-domain or licensed text ingested

---

## 🎯 What "MASSIVE win" means (John's framing)

> *"We're building an investigation tool. Christianity is without a doubt where we can find the most characters and info — and from there we also will be able to trace back other influences and gnostic paths. If we any moment can establish a connection between Christianity and an older religion from Egypt or whatever, THAT for us is a MASSIVE victory."*

The 10 load-bearing transmission edges already in the vault (per [`STATUS.md`](00_meta/STATUS.md)):

1. Stoic logos → Philo → John → Justin
2. Plato → Justin / Clement / Origen
3. Plato (Timaeus) → Valentinus
4. Pythagoras → Valentinian Pleroma numerology
5. Aristotle (Categories) → Basilides
6. Egyptian solar numerology → Basilidean 365-heavens
7. Mitra-Vedic → Mithra-Zoroastrian → Mithras-Roman
8. Isis-Hellenistic → Mary-Theotokos iconography
9. Hermetic Corpus → Clement + Origen (the strongest 0–300 CE edge)
10. Philo's allegory → Origen's allegory

Plus the Ethiopian Ge'ez 1 Enoch preservation chain, the Greco-Buddhist Aśokan-edicts wedge, the Avicenna → Aquinas Aristotelian transmission, and many more added in subsequent batches. **When you stub a node, hunt for the older-tradition transmission edge and flag it explicitly with a peer-reviewed source.**

---

## 🧭 Quick links

- Pre-flight: [`DASHBOARD.md`](00_meta/DASHBOARD.md) · [`ACTIVE-AGENTS.md`](00_meta/ACTIVE-AGENTS.md) · [`STATUS.md`](00_meta/STATUS.md) · per-session archives in [`00_meta/agents-archive/`](00_meta/agents-archive/) (full historical claim blocks)
- Linting: `python3 lint_yaml.py` (catches malformed frontmatter / slug drift / date inversions / unresolved wikilinks; `--strict` exits 1 on ERROR; ~0.3s on the full vault)
- Naming: [`canonical-slugs.md`](00_meta/canonical-slugs.md)
- Methodology: [`methodology.md`](00_meta/methodology.md) · [`source-integrity.md`](00_meta/source-integrity.md)
- **App-code agents only:** [`app-architecture.md`](00_meta/app-architecture.md) (load-bearing rules doc — type-scale tokens, motion tokens, component primitives, anti-patterns, view-extension contract, Scripture-as-polish-tier-ceiling)
- Schemas: [`schema-deity.md`](00_meta/schema-deity.md) · [`schema-document.md`](00_meta/schema-document.md) · [`schema-event.md`](00_meta/schema-event.md) · [`schema-person.md`](00_meta/schema-person.md) · [`schema-symbol.md`](00_meta/schema-symbol.md) · [`schema-theme.md`](00_meta/schema-theme.md) · [`schema-tradition.md`](00_meta/schema-tradition.md)
- Standing audits: [`AUDIT/00_AUDIT_OVERVIEW.md`](AUDIT/00_AUDIT_OVERVIEW.md)
- Strategic context: [`HANDOFF.md`](00_meta/HANDOFF.md)
- App usage: [`app-usage.md`](00_meta/app-usage.md)
